server:
  port: 8081

jwt:
  secret: urieuryeiur

data:
  database:
    users: Users
    relationships: Relationship
    message: Messages
    channel: ChatChannel
    constraints: Constraint

  constraints:
    usernameConst: Username
    userEmailConst: UserEmail